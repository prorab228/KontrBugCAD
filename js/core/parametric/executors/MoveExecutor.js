import*as THREE from"three";import{ParametricExecutor}from"./ParametricExecutor.js";export class MoveExecutor extends ParametricExecutor{execute(r,o){const{target:t,position:e,objects:i}=r.parameters;if(i)for(const r of i){const t=o.objectMap.get(r.uuid);t&&t.position.fromArray(r.position)}else{if(!t||!e)throw new Error("Move: неверные параметры операции");{const r=o.objectMap.get(t);if(!r)throw new Error(`Move: объект ${t} не найден`);r.position.fromArray(e)}}return null}}